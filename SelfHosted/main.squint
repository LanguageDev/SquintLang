import System.Console.WriteLine;
import System.IO.File;
import System.Collections.Generic.List[T];

func main() {
    var text = File.ReadAllText("lexer.squint");
    val tokens = Lexer.tokenize(text);
    WriteLine(tokens.Count);
}
